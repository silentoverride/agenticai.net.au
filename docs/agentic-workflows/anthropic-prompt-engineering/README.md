# Anthropic Prompt Engineering Prompt Collection

Source document: `/home/loki/Documents/nate/Anthropic Prompt Engineering.md`
Folder: `docs/agentic-workflows/anthropic-prompt-engineering/`
Original source folder name: `nate-anthropic-prompt-engineering`

## Purpose

This folder contains the agentic workflow prompt files extracted from this source document. The folder name is descriptive of the prompt set's purpose, function, or intended use. Each prompt file uses an abbreviation derived from this folder name, with numbering scoped to this folder.

## How to use these prompts

Open the prompt file you need, copy the prompt content into an AI assistant, and provide the context requested by the prompt. Use the sequence number to preserve ordering within this folder.

## Prompt files

| Document ID | File | Prompt | Purpose |
| --- | --- | --- | --- |
| — | — | — | No extractable prompts were found for this source document. |

## Naming convention

Files use the pattern `ape-[sequence]-v[version]-[h2-title].md`, where `ape` is the abbreviation derived from `anthropic-prompt-engineering`, the three-digit sequence preserves the prompt order within this folder, and `v1` is the initial document version.

Document IDs use the pattern `anthropic-prompt-engineering-[sequence]-v1`, where `anthropic-prompt-engineering` is this prompt file's immediate parent folder and the three-digit sequence preserves prompt order within this folder.
