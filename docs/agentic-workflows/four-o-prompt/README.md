# 4o Prompt Prompt Collection

Source document: `/home/loki/Documents/nate/4o Prompt.md`
Folder: `docs/agentic-workflows/four-o-prompt/`
Original source folder name: `nate-4o-prompt`

## Purpose

This folder contains the agentic workflow prompt files extracted from this source document. The folder name is descriptive of the prompt set's purpose, function, or intended use. Each prompt file uses an abbreviation derived from this folder name, with numbering scoped to this folder.

## How to use these prompts

Open the prompt file you need, copy the prompt content into an AI assistant, and provide the context requested by the prompt. Use the sequence number to preserve ordering within this folder.

## Prompt files

| Document ID | File | Prompt | Purpose |
| --- | --- | --- | --- |
| `four-o-prompt-001-v1` | `fop-001-v1-4o-prompt.md` | 4o Prompt | Perfect. Let’s raise the bar and make this bulletproof. |

## Naming convention

Files use the pattern `fop-[sequence]-v[version]-[h2-title].md`, where `fop` is the abbreviation derived from `four-o-prompt`, the three-digit sequence preserves the prompt order within this folder, and `v1` is the initial document version.

Document IDs use the pattern `four-o-prompt-[sequence]-v1`, where `four-o-prompt` is this prompt file's immediate parent folder and the three-digit sequence preserves prompt order within this folder.
