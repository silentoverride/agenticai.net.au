# Prompting best practices Prompt Collection

Source document: `/home/loki/Documents/nate/claude-prompting-best-practices.md`
Folder: `docs/agentic-workflows/claude-prompting-best-practices/`
Original source folder name: `nate-claude-prompting-best-practices`

## Purpose

This folder contains the agentic workflow prompt files extracted from this source document. The folder name is descriptive of the prompt set's purpose, function, or intended use. Each prompt file uses an abbreviation derived from this folder name, with numbering scoped to this folder.

## How to use these prompts

Open the prompt file you need, copy the prompt content into an AI assistant, and provide the context requested by the prompt. Use the sequence number to preserve ordering within this folder.

## Prompt files

| Document ID | File | Prompt | Purpose |
| --- | --- | --- | --- |
| — | — | — | No extractable prompts were found for this source document. |

## Naming convention

Files use the pattern `cpbp-[sequence]-v[version]-[h2-title].md`, where `cpbp` is the abbreviation derived from `claude-prompting-best-practices`, the three-digit sequence preserves the prompt order within this folder, and `v1` is the initial document version.

Document IDs use the pattern `claude-prompting-best-practices-[sequence]-v1`, where `claude-prompting-best-practices` is this prompt file's immediate parent folder and the three-digit sequence preserves prompt order within this folder.
